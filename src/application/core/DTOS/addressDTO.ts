/* eslint-disable prettier/prettier */
export interface AddressDTO {
  uf: string;
  city: string;
  district: string;
  street: string;
  complement: string;
  number: number;
  reference: string;
}
