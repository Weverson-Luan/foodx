/* eslint-disable prettier/prettier */
export interface AddressDTO {
  id: string;
  user_id: string;
  uf: string;
  zip_code: string;
  city: string;
  district: string;
  street: string;
  complement: string;
  number: number;
  reference: string;
  long: number;
  lati: number;
}
